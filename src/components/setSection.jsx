import React, {useState} from 'react';

const setSection = () => {
    const [section, setSection] = useState(1);
    setSection(section = document.getElementById())
    return (
        <>
        </>
    )
}

export default setSection;